# Core API runtime.
#
# Deliberately excludes the machine-learning stack: the classifier imports
# ultralytics lazily and degrades to ("unknown", 0.0) when it is absent, so the
# API boots and serves every endpoint without a ~2GB dependency tree.
# Install requirements-ml.txt to enable local model inference.
fastapi==0.115.6
uvicorn==0.30.6
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.12
requests==2.32.3
openai==1.57.0
