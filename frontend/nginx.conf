server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: unknown paths are handled by the client router.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
